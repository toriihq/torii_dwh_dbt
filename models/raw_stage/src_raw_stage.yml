version: 2

sources:
  - name: source_name
    database: |
      {%- if  target.name == "raw_stage_dev" -%} raw_stage_dev
      {%- elif target.name == "raw_stage_prod"  -%} raw_stage
      {%- else -%} invalid_database
      {%- endif -%}
    schema: source_schema